# Dependencies — reinstalled from package-lock.json, never commit
node_modules/

# Compiled output — regenerated by `npx tsc` from src/
dist/

# Environment secrets (if you add any later)
.env
.env.local

# Logs & OS cruft
*.log
.DS_Store
server.log